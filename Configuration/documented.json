{
    "name": "Laynes development configuration.",
    "description": "The configuration file for laynes bot, during the development process.",
    "connection": {
        "server": {
            "sever": "irc.freenode.org",
            "port": 6667,
            "maxReconnects": 1
        },
        "identity": {
            "name": "laynes-bot",
            "nick": "laynes-bot"
        },
        "channels": [
            "layne-test"
        ]
    },
    "commands": {
        "packages": [

        ],
        "commands": [
            "join",
            "poke",
            "quit",
            "reboot",
            "restart",
            "say",
            "timeout",
            "part"
        ]
    },
    "commandPrefix": "!",
    // Define error handling.
    "errors": {
        "reporting": true, // false if errors should be ignored where possible.
        "display": true,   // true if you want to see errors.
        "logging": false   // true if you want to log errors via logging. Remeber to enable logging.
    },
    "logging": {
        "enabled": true,   // true if logging should be enabled.
        "file": "/Users/daniel/Sites/dev/irc/irc-bot/logs/test-" // The file containing the log.
    }
}